# Linux DO 大模型排行榜 —— 技术方案

> 版本：v0.3
> 状态：已根据第二轮评审更新（移除外链讨论帖，定位为独立站点）

---

## 一、项目定位

面向 Linux DO 社区的、由用户投票产生的大模型排行榜站点。**只做榜单，不做试炼场**。

核心价值：
- 一处看清各家模型在不同维度上的真实口碑
- 评价由社区真人给出，而非厂商自卖自夸
- 讨论沉淀在本站，形成独立的社区氛围

---

## 二、技术选型

| 层 | 选型 | 理由 |
|----|------|------|
| 框架 | Next.js 14（App Router） | Vercel 原生支持，前后端一体 |
| 语言 | TypeScript | 类型安全 |
| UI | Tailwind CSS + shadcn/ui | 快速搭建，风格统一 |
| 数据库 | PostgreSQL（Neon / Vercel Postgres） | 免费额度够初期使用 |
| ORM | Drizzle ORM | 轻量、类型友好、迁移直观 |
| 认证 | Auth.js（NextAuth v5）+ 自定义 Linux DO OAuth Provider | 社区流量入口 |
| 部署 | Vercel | 零配置，自动 CI/CD |
| 源码托管 | GitHub | 配合 Vercel 自动部署 |
| 缓存（可选） | Vercel KV / Upstash Redis | 排行榜结果缓存 |

---

## 三、核心概念

### 3.1 三层分类结构

```
大类（开源属性）
  └─ 分类（模态）
       └─ 模型
            └─ 多维度评分
```

- **大类**：开源 / 非开源（两个）
- **分类**：文字模型 / 生图模型 / 生视频模型（可扩展）
- **模型**：具体一个模型条目，归属某大类下的某分类
- **维度**：每个分类有自己的一组评分维度

### 3.2 各分类的维度（初版）

| 分类 | 维度 |
|------|------|
| 文字模型 | 代码能力、RP 能力、任务执行、推理能力、长文本、中文表达 |
| 生图模型 | 写实度、艺术性、Prompt 遵循、出图速度 |
| 生视频模型 | 画面连贯性、画质、Prompt 遵循、时长能力 |

> 维度由后台可配置，不写死在代码里。

### 3.3 模型生命周期

```
[草稿] → [观察区] → [正式榜] → [归档]
```

| 状态 | 说明 | 进入条件 |
|------|------|----------|
| 草稿 | 仅管理员可见 | 后台新建 |
| 观察区 | 站点显示「新」标签并置顶；可投票，但不计入排行 | 管理员发布 |
| 正式榜 | 计入排行，参与排序 | 发布时间 ≥ 7 天 **或** 总票数 ≥ 50 |
| 归档 | 仅在历史页面可见 | 管理员手动归档 |

---

## 四、评分与排序机制

### 4.1 评分规则

- **范围**：0–10 整数
- **0 分含义**：等同"弃权 / 不熟悉"，**不计入均分**
- **粒度**：用户可针对每个维度独立打分；允许只评一部分维度
- **修改**：允许修改和撤回，每次修改记录历史
- **频次**：每个用户对每个模型每个维度只能有一条当前有效评分

### 4.2 准入门槛（防刷）

依赖 Linux DO OAuth 返回的 `trust_level`：

| 等级 | 浏览 | 评论 | 投票 |
|------|------|------|------|
| 0（新用户） | ✅ | ✅ | ❌ |
| 1+ | ✅ | ✅ | ✅ |

> 此门槛可在后台调整。

### 4.3 排序算法：贝叶斯平均

直接用算术平均会让"3 个人投全 10 分"的模型霸榜。改用贝叶斯平均做排序：

```
weighted_score = (C × m + n × avg) / (C + n)

n   = 该维度的有效投票数（不含 0 分）
avg = 该维度的算术均分
m   = 该分类下、该维度的全站均分
C   = 平滑系数，初始设为 30
```

- **页面展示**：真实均分（用户感知直观）
- **背后排序**：贝叶斯均分（公平）
- 排行表格里同时显示"票数"列，让用户自己判断可信度

### 4.4 综合分

```
综合分 = 各维度贝叶斯均分的平均（默认等权）
```

**进阶（第二阶段）**：用户可在前端拖滑块自定义权重（例如"代码能力 ×2、RP ×0.5"），表格实时按个人偏好重排。该功能完全在前端计算，不写回数据库。

---

## 五、页面结构

### 5.1 路由设计

```
/                                  → 首页（重定向到 /rankings/open-source/text）
/rankings/[license]/[category]     → 排行榜主页
   - license:  open-source | closed-source
   - category: text | image | video
/models/[id]                       → 模型详情页
/login                             → 登录页
/profile                           → 个人中心（我的投票）
/admin/*                           → 后台（仅管理员）
/api/auth/[...nextauth]            → 认证回调
/api/...                           → 业务 API
```

### 5.2 排行榜页（核心页）

```
┌───────────────────────────────────────────────────────────┐
│   Logo  | 排行榜 | 关于  ( Linux DO 头像 / 登录 )            │
├───────────────────────────────────────────────────────────┤
│                                                            │
│   [ 开源 ●● 非开源 ]    ← 大类 Tab（pill 风格）            │
│   [ 文字 | 生图 | 生视频 ]   ← 二级 Tab                    │
│                                                            │
│   🆕 观察区（2 个新模型）  ▼ 展开                           │
│                                                            │
│  ┌──────┬──────┬─────┬─────┬─────┬──────┬─────┐          │
│  │ 模型 │ 代码↕│ RP↕ │ 任务↕│推理↕│综合↕↓│票数 │          │
│  ├──────┼──────┼─────┼─────┼─────┼──────┼─────┤          │
│  │GPT-5 │ 9.2  │ 7.8 │ 8.9 │ 9.0 │ 8.7  │1234 │ → 详情   │
│  │Claude│ 8.9  │ 9.4 │ 8.5 │ 8.8 │ 8.9  │ 987 │          │
│  │ ...                                                    │
│  └──────┴──────┴─────┴─────┴─────┴──────┴─────┘          │
│                                                            │
│  默认按综合分降序                                          │
└───────────────────────────────────────────────────────────┘
```

排序交互：
- 点击列头：第一次降序，第二次升序，第三次回到默认（综合分降序）
- 当前排序列高亮 + 显示 ↑/↓ 箭头
- URL 同步排序状态（`?sort=code&order=desc`），方便分享

### 5.3 移动端

表格变卡片列表：

```
┌────────────────────────────┐
│ GPT-5          综合 8.7 ★  │
│ ──────────────────────     │
│ 代码  ████████░░  9.2      │
│ RP    ███████░░░  7.8      │
│ 任务  ████████░░  8.9      │
│ 1234 票           [详情]   │
└────────────────────────────┘
```

顶部固定一个"排序方式"下拉选择按哪个维度排。

### 5.4 模型详情页

- 模型基本信息（logo、官网、所属机构、参数量、上下文长度等元数据）
- 各维度评分柱状图 + 票数 + 评分分布直方图
- 评分趋势曲线（按周聚合）
- 用户对该模型的评价摘要
- 站内评论区（见 5.5）
- 我的评分（已登录时显示，可修改）

### 5.5 评论 / 留言

每个模型详情页底部带评论区。规则：

**准入：**
- 浏览评论：不限
- 发表评论：登录 + `trust_level >= 1`（与投票门槛一致）

**功能：**
- 一级评论 + 二级回复（最多两层，避免过深嵌套）
- 支持 Markdown（受限子集：粗体、斜体、行内代码、代码块、链接、引用），不允许 HTML
- 点赞 / 点踩
- 排序：默认"按热度"（点赞数 - 时间衰减），可切换"最新"
- 删除：作者本人 / 管理员
- 编辑：作者本人 15 分钟内可编辑，编辑后显示"已编辑"标记

**反垃圾与审核：**
- 频率限制：单用户每分钟 ≤ 3 条评论
- 关键字过滤（黑名单可在后台维护）
- 用户举报按钮，被举报次数达阈值自动隐藏并进入审核队列
- 后台审核面板：放行 / 删除 / 封禁作者
- 自动隐藏规则：被点踩数 - 点赞数 ≥ N 时折叠（用户可点击展开）

> 评论功能涉及反垃圾、审核工作量较大，**建议放在第二阶段开发**：第一阶段先把核心榜单跑稳定，第二阶段再补站内评论。如要在第一阶段同时上线，开发周期需要再加 1–2 周。

---

## 六、数据库 Schema

```ts
// 用户
users {
  id              uuid PK
  linuxdo_id      bigint UNIQUE         // Linux DO 用户 ID
  username        text
  display_name    text
  avatar_url      text
  trust_level     int                   // 0-4
  is_admin        boolean default false
  is_banned       boolean default false
  created_at      timestamptz
  last_login_at   timestamptz
}

// 大类（开源 / 非开源）
licenses {
  id     int PK
  slug   text UNIQUE                    // open-source | closed-source
  name   text                           // 开源 | 非开源
  order  int
}

// 分类（文字 / 生图 / 生视频）
categories {
  id     int PK
  slug   text UNIQUE                    // text | image | video
  name   text                           // 文字模型 | 生图模型 | 生视频模型
  order  int
}

// 维度（按分类配置）
dimensions {
  id           int PK
  category_id  int FK -> categories
  slug         text                     // code | rp | task | ...
  name         text                     // 代码能力 | RP 能力 | ...
  description  text
  order        int
  UNIQUE(category_id, slug)
}

// 模型
models {
  id              uuid PK
  license_id      int FK -> licenses
  category_id     int FK -> categories
  name            text                  // GPT-5
  slug            text UNIQUE           // gpt-5
  vendor          text                  // OpenAI
  logo_url        text
  homepage_url    text
  description     text
  context_length  int                   // 文字模型用
  params          text                  // 参数量描述，如 "70B"
  released_at     date
  status          text                  // draft | observing | listed | archived
  pinned          boolean default false // 是否在观察区置顶
  published_at    timestamptz           // 进入观察区的时间
  created_at      timestamptz
  updated_at      timestamptz
}

// 评分（每用户对每模型每维度一条当前记录）
votes {
  id           uuid PK
  user_id      uuid FK -> users
  model_id     uuid FK -> models
  dimension_id int FK -> dimensions
  score        int                      // 1-10（0 分代表撤回，单独表示）
  created_at   timestamptz
  updated_at   timestamptz
  UNIQUE(user_id, model_id, dimension_id)
}

// 评分历史（每次修改追加一条，便于审计 / 反刷）
vote_history {
  id           uuid PK
  vote_id      uuid FK -> votes
  user_id      uuid
  model_id     uuid
  dimension_id int
  score        int
  action       text                     // create | update | withdraw
  created_at   timestamptz
  ip_hash      text                     // 仅存哈希，用于反刷
  user_agent   text
}

// 模型评分聚合缓存（每小时定时刷新或写入时增量更新）
model_stats {
  model_id      uuid
  dimension_id  int
  vote_count    int                     // 有效票数（不含 0）
  avg_score     numeric(4,2)            // 真实均分
  weighted_score numeric(4,2)           // 贝叶斯均分
  updated_at    timestamptz
  PRIMARY KEY(model_id, dimension_id)
}

// 操作日志（后台用）
audit_logs {
  id          uuid PK
  actor_id    uuid FK -> users
  action      text                      // model.create | model.update | user.ban | ...
  target_type text
  target_id   text
  payload     jsonb
  created_at  timestamptz
}

// 评论
comments {
  id              uuid PK
  model_id        uuid FK -> models
  user_id         uuid FK -> users
  parent_id       uuid FK -> comments NULL  // 二级回复时指向父评论
  content         text                       // Markdown 原文
  content_html    text                       // 渲染并清洗后的 HTML（缓存）
  like_count      int default 0
  dislike_count   int default 0
  report_count    int default 0
  is_hidden       boolean default false      // 被举报或人工隐藏
  is_deleted      boolean default false      // 软删除
  edited_at       timestamptz NULL
  created_at      timestamptz
  ip_hash         text
  INDEX(model_id, created_at)
  INDEX(user_id)
}

// 评论点赞 / 点踩
comment_reactions {
  id          uuid PK
  comment_id  uuid FK -> comments
  user_id     uuid FK -> users
  reaction    text                           // like | dislike
  created_at  timestamptz
  UNIQUE(comment_id, user_id)
}

// 评论举报
comment_reports {
  id          uuid PK
  comment_id  uuid FK -> comments
  reporter_id uuid FK -> users
  reason      text                           // spam | abuse | off_topic | other
  detail      text
  status      text                           // pending | resolved | rejected
  handled_by  uuid FK -> users NULL
  handled_at  timestamptz NULL
  created_at  timestamptz
  UNIQUE(comment_id, reporter_id)
}

// 关键字黑名单（后台可维护）
banned_keywords {
  id          int PK
  pattern     text                           // 普通字符串 / 正则
  is_regex    boolean default false
  action      text                           // block（拒绝发布） | hide（自动隐藏）
  created_by  uuid FK -> users
  created_at  timestamptz
}
```

---

## 七、API 设计

约定：JSON 接口，认证用 Auth.js session cookie。

### 7.1 公开接口

```
GET  /api/rankings?license=open-source&category=text
       → 返回当前榜单的所有正式模型 + 各维度统计

GET  /api/rankings/observing?license=open-source&category=text
       → 观察区模型（带「新」标签）

GET  /api/models/:slug
       → 模型详情 + 各维度评分 + 趋势数据

GET  /api/models/:slug/stats?range=30d
       → 趋势数据

GET  /api/models/:slug/comments?sort=hot&page=1
       → 模型评论列表（默认按热度，可选 latest）
```

### 7.2 用户接口（需登录）

```
GET  /api/me                          → 当前用户信息
GET  /api/me/votes?model=:slug        → 我对某模型的评分
POST /api/votes
       body: { modelId, scores: [{ dimensionId, score }] }
       → 批量提交 / 更新评分
DELETE /api/votes/:modelId/:dimensionId
       → 撤回某维度评分

POST   /api/comments
       body: { modelId, parentId?, content }
       → 发表评论 / 回复（trust_level >= 1）
PATCH  /api/comments/:id              → 编辑（作者本人 15 分钟内）
DELETE /api/comments/:id              → 删除（作者本人 / 管理员）
POST   /api/comments/:id/reaction
       body: { reaction: 'like' | 'dislike' | null }
       → 点赞 / 点踩 / 取消
POST   /api/comments/:id/report
       body: { reason, detail? }
       → 举报
```

### 7.3 管理员接口（需 is_admin）

```
POST   /api/admin/models              → 新建模型
PATCH  /api/admin/models/:id          → 修改
PATCH  /api/admin/models/:id/status   → 状态切换（草稿→观察区→正式榜→归档）
DELETE /api/admin/models/:id          → 删除（软删除）

GET    /api/admin/dimensions
POST   /api/admin/dimensions
PATCH  /api/admin/dimensions/:id

POST   /api/admin/users/:id/ban
POST   /api/admin/users/:id/unban

GET    /api/admin/audit-logs
GET    /api/admin/votes/suspicious    → 可疑投票（如同 IP 段集中投票）

GET    /api/admin/comments?status=hidden|reported|all
PATCH  /api/admin/comments/:id        → 强制隐藏 / 恢复 / 删除
GET    /api/admin/reports?status=pending
PATCH  /api/admin/reports/:id         → 处理举报（resolved / rejected）

GET    /api/admin/banned-keywords
POST   /api/admin/banned-keywords
DELETE /api/admin/banned-keywords/:id
```

---

## 八、Linux DO OAuth 接入

Linux DO 的 OAuth2 端点（`connect.linux.do`）：

```
授权:    GET  https://connect.linux.do/oauth2/authorize
换 token: POST https://connect.linux.do/oauth2/token
用户信息: GET  https://connect.linux.do/api/user
```

返回字段中包含 `trust_level`，作为投票门槛依据。

接入方式：在 Auth.js 中实现一个自定义 `OAuthProvider`：

```ts
// auth.config.ts 伪代码
const LinuxDoProvider = {
  id: "linuxdo",
  name: "Linux DO",
  type: "oauth",
  authorization: "https://connect.linux.do/oauth2/authorize",
  token:        "https://connect.linux.do/oauth2/token",
  userinfo:     "https://connect.linux.do/api/user",
  clientId:     process.env.LINUXDO_CLIENT_ID,
  clientSecret: process.env.LINUXDO_CLIENT_SECRET,
  profile(p) {
    return {
      id: p.id,
      name: p.username,
      image: p.avatar_url,
      trust_level: p.trust_level,
    };
  },
};
```

每次登录时把 `trust_level` 同步进 `users` 表。

---

## 九、后台

**第一阶段**：直接用 Drizzle Studio / Postgres GUI（如 TablePlus、DBeaver）作为后台，省去自建后台界面的工作量。

**第二阶段**：自建 `/admin` 页面，包含：

- 模型管理（CRUD + 状态切换 + 拖拽排序观察区置顶）
- 维度管理（按分类编辑维度）
- 用户管理（搜索、封禁、查看投票历史）
- 评分异常检测面板（同 IP 段投票、短时间集中投同一模型等）
- 审计日志

后台用 shadcn/ui 的 Data Table 组件，开发量可控。

---

## 十、防刷策略

最低限度：

1. **登录门槛**：必须 Linux DO 登录，且 `trust_level >= 1` 才能投票
2. **唯一性约束**：数据库层 `UNIQUE(user_id, model_id, dimension_id)` 防止同一用户对同维度重复投
3. **审计字段**：`vote_history` 记录 IP 哈希、UA、时间戳
4. **频次限制**：单用户每分钟最多 N 次投票请求（中间件层）
5. **观察期保护**：观察区不计入排名，新模型刷高分也不会立即上榜
6. **管理员复核**：后台异常面板 + 一键撤销某用户全部投票

进阶（暂不做，按需启用）：

- 引入 `trust_level` 加权（高级别用户的票分量更大）
- 接入 Cloudflare Turnstile 抵御自动化脚本

---

## 十一、性能与缓存

- **榜单读取热**：`/api/rankings` 可能每秒被大量请求 → **结果缓存到 KV，TTL 60s**
- **聚合统计**：`model_stats` 表通过定时任务（Vercel Cron）每 5 分钟全量重算一次；写投票时同步增量更新（双保险）
- **首屏渲染**：榜单页用 Next.js ISR（Incremental Static Regeneration），revalidate 60s

---

## 十二、部署

```
GitHub repo
    │
    │ push
    ▼
Vercel (build & deploy)
    │
    ├── Next.js Server (Edge / Node Functions)
    ├── Vercel KV (缓存，可选)
    └── Vercel Cron (定时任务)
            │
            ▼
        Neon Postgres (主数据库)
```

环境变量清单：

```
DATABASE_URL              Neon 连接串
NEXTAUTH_URL              站点 URL
NEXTAUTH_SECRET           随机长字符串
LINUXDO_CLIENT_ID         Linux DO 应用 ID
LINUXDO_CLIENT_SECRET     Linux DO 应用密钥
ADMIN_LINUXDO_IDS         初始管理员的 Linux DO ID 列表（逗号分隔）
KV_REST_API_URL           （可选）
KV_REST_API_TOKEN         （可选）
```

---

## 十三、MVP 实施路线

### 第一阶段：核心榜单（2–3 周）

- [ ] Linux DO OAuth 登录
- [ ] 数据库 schema + 种子数据（先填几个常见模型）
- [ ] 大类 / 分类 Tab + 表格排行榜（桌面）
- [ ] 投票（0–10）+ 贝叶斯排序
- [ ] 模型详情页（评分图表 + 基本信息）
- [ ] 移动端卡片视图
- [ ] 简易管理（用 Drizzle Studio 操作数据库）

### 第二阶段：完善体验（2–3 周）

- [ ] 观察区 + 自动晋升机制（7 天 或 50 票，满足任一）
- [ ] **站内评论区**（含点赞 / 点踩 / 举报 / Markdown）
- [ ] 自建 `/admin` 后台界面（模型 / 维度 / 用户 / 评论审核 / 关键字）
- [ ] 评分异常检测面板
- [ ] 用户个人中心（我的评分历史 / 我的评论）
- [ ] 自定义维度权重排序

### 第三阶段：进阶（按需）

- [ ] 评分趋势图表
- [ ] 模型对比页（两个模型并排）
- [ ] 数据导出 / 公开 API
- [ ] 评分按用户 trust_level 加权（若发现刷票严重再启用）

---

## 十四、决策记录

第一轮评审（v0.1 → v0.2）已确认：

| # | 问题 | 决策 |
|---|------|------|
| 1 | 评论用站内还是外链？ | ✅ **站内评论**（本站为独立站点，不挂外链） |
| 2 | 投票门槛 | ✅ `trust_level >= 1` |
| 3 | 各分类初始维度 | ✅ 暂用第 3.2 节列出的维度，后台可调整 |
| 4 | 综合分用户自定义权重 | ✅ 放第二阶段 |
| 5 | 第一阶段后台 | ✅ 用 Drizzle Studio 凑合 |
| 6 | 观察区准入门槛 | ✅ 改为"或"逻辑：发布 ≥ 7 天 **或** 总票数 ≥ 50，满足任一即可 |
| 7 | 模型对比功能 | ✅ 放第三阶段 |
| 8 | 数据库 | ✅ Neon |

待你最终确认后即可开始搭脚手架。

---

## 十五、不做的事（明确边界）

- ❌ 大模型试炼场 / API 调用代理
- ❌ 厂商榜单（不做"OpenAI vs Anthropic 综合实力"这类企业排名）
- ❌ Benchmark 跑分聚合（与 LMArena、Chatbot Arena 区别开，本站只做主观投票）
- ❌ 收费 / 会员 / 付费推广

---

> v0.2 已根据第一轮评审更新。如无其他修改意见，下一步开始搭脚手架（Next.js + Drizzle + Neon + Linux DO OAuth 骨架）。
